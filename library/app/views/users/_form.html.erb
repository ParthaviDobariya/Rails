<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">
    <div class="row">
      <div class="col-sm-9 col-md-7 col-lg-8 mx-auto">
        <div class="card">
          <div class="card-body">
            <div class="field form-group row">
              <%= form.label :name, class: "col-6 col-form-label" %>
              <div class="col-6">
                <%= form.text_field :name, class: "form-control" %>
              </div>
            </div>

            <div class="field form-group row">
              <%= form.label :mobile_no, class: "col-6 col-form-label" %>
              <div class="col-6">
                <%= form.text_field :mobile_no, class: "form-control" %>
              </div>
            </div>

            <div class="field form-group row">
              <%= form.label :email, class: "col-6 col-form-label" %>
              <div class="col-6">
                <%= form.text_field :email, class: "form-control" %>
              </div>
            </div>

            <div class="field form-group row">
              <%= form.label :address, class: "col-6 col-form-label" %>
              <div class="col-6">
                <%= form.text_field :address, class: "form-control" %>
              </div>
            </div>

            <div class="actions">
              <%= form.submit %>
            </div>

          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
